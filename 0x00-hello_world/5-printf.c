#include <stdio.h>

/**
 * main - outputs line of text
 *
 * outputs line of text with printf function
 * Return: returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
